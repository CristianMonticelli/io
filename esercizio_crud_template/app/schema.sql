-- Schema del database (vuoto - riempire domani)
-- ISTRUZIONI: Crea la tabella con i campi che ti servono

CREATE TABLE IF NOT EXISTS items (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    -- AGGIUNGERE I CAMPI QUI DURANTE LA VERIFICA
    -- Esempio: name TEXT NOT NULL,
    -- Esempio: description TEXT,
    -- Esempio: created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
